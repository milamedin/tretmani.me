{
  "name": "tretmani-me",
  "version": "1.0.0",
  "private": true,
  "description": "Svi saloni Crne Gore na jednom mjestu",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "dev": "node scripts/build.js && npx --yes serve dist -p 4340 --no-clipboard"
  },
  "engines": {
    "node": ">=18"
  }
}
